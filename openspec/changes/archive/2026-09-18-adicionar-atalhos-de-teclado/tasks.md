## 1. Comandos no Manifest

- [x] 1.1 Declarar a chave `commands` em `wxt.config.ts` com o comando reservado de ação (`default: Ctrl+Shift+K`, `mac: Command+Shift+K`, sem `description`) e `open-task-manager` (`default: Ctrl+Shift+L`, `mac: Command+Shift+L`, `description` em pt-BR), sem marcar nenhum como global; verificar pelo `wxt build` que o `manifest.json` gerado contém exatamente esses dois comandos.
- [x] 1.2 Estender `tests/manifest/manifest-permissions.test.ts` (ou adicionar um teste irmão em `tests/manifest/`) cobrindo: os dois comandos declarados com as combinações esperadas por plataforma, ausência de qualquer terceiro comando, ausência de comando sem `suggested_key`, ausência de `"global"`, ausência de `Ctrl+Alt` em qualquer combinação e `MacCtrl` restrito à chave `mac`; verificar que `npm run test` passa.
- [x] 1.3 Confirmar no mesmo arquivo de teste que a lista de permissões permanece exatamente `activeTab`, `alarms`, `contextMenus`, `notifications`, `sidePanel` e `storage`, sem permissões opcionais, sem `host_permissions` e sem content scripts; verificar que o teste existente continua verde sem alteração das constantes de permissão.

## 2. Comando de gerenciamento no service worker

- [x] 2.1 Registrar em `src/entrypoints/background.ts` um listener síncrono de `commands.onCommand` que ignora comandos diferentes de `open-task-manager` e, para esse comando, chama `openSidePanelInWindow` com o `windowId` da aba recebida antes de qualquer `await`; verificar pelo teste de 2.2.
- [x] 2.2 Adicionar em `tests/entrypoints/background.test.ts`, usando `fakeBrowser`, os casos: comando desconhecido não abre nada; `open-task-manager` abre o Side Panel com o `windowId` da aba recebida; aba ou `windowId` ausentes não abrem nenhum painel e registram log; rejeição de `sidePanel.open` é tratada sem lançar; verificar que `npm run test` passa.
- [x] 2.3 Cobrir no mesmo teste que `sidePanel.open` é invocado de forma síncrona pelo handler, antes de qualquer promessa ser aguardada — por exemplo, afirmando a chamada imediatamente após invocar o listener e antes de `flushPromises`; verificar que o teste falha se um `await` for inserido antes da abertura.
- [x] 2.4 Garantir que o log de falha desse caminho não inclui título, descrição, URL de origem nem qualquer outro conteúdo de tarefa, seguindo o padrão já usado na captura; verificar por asserção sobre o argumento passado a `console.error`.

## 3. Leitura dos atalhos efetivos

- [x] 3.1 Criar em `src/application/` a porta de leitura dos atalhos, com o tipo de resultado por ação e a representação explícita de "sem atalho", sem referência a APIs do Chrome; verificar que `tests/architecture/layer-boundaries.test.ts` continua verde.
- [x] 3.2 Criar em `src/infrastructure/chrome/` o adapter que implementa essa porta consultando `commands.getAll()`, traduzindo combinação vazia em "sem atalho" e expondo também a ação de abrir `chrome://extensions/shortcuts` por `tabs.create`; verificar pelo teste de 3.3.
- [x] 3.3 Adicionar `tests/infrastructure/chrome-shortcuts-reader.test.ts` com `fakeBrowser` cobrindo: os dois comandos com atalho; um comando com `shortcut` vazio; rejeição de `commands.getAll`; e a abertura da página de personalização em uma nova aba; verificar que `npm run test` passa.

## 4. Bloco de atalhos no Side Panel

- [x] 4.1 Criar o componente do bloco de atalhos em `src/components/`, recebendo a porta por `inject`, consultando os atalhos ao ser apresentado, exibindo o nome de cada ação com a combinação em vigor, sinalizando explicitamente comando sem atalho, informando falha de consulta sem quebrar a listagem e oferecendo a ação de personalizar; verificar pelo teste de 4.3.
- [x] 4.2 Apresentar o bloco ao fim da listagem em `src/components/tasks/TaskManager.vue`, sem criar novo valor de `mode` e sem acrescentar botão ao cabeçalho, e prover o adapter em `src/entrypoints/sidepanel/main.ts` no mesmo padrão dos demais serviços; verificar que `tests/components/tasks/TaskManager.test.ts` continua verde.
- [x] 4.3 Adicionar `tests/components/shortcuts/ShortcutsHint.test.ts` (ajustando o nome ao componente criado) com um fake da porta, cobrindo: ambos com atalho; um sem atalho; combinação alterada refletida em nova apresentação; falha da consulta; e acionamento da personalização pelo teclado; verificar que `npm run test` passa.
- [x] 4.4 Revisar os textos do bloco em pt-BR com acentuação correta, conferir alcance e acionamento por teclado e confirmar que as cores usadas já estão cobertas por `tests/styles/contrast.test.ts`, estendendo esse teste se o bloco introduzir alguma cor nova; verificar que `npm run test` passa.

## 5. Documentação

- [x] 5.1 Atualizar o `README.md` removendo a afirmação de que não existe atalho de teclado, descrevendo as duas combinações por plataforma, a personalização em `chrome://extensions/shortcuts` e o comportamento quando a combinação já está ocupada; verificar por leitura que nenhuma outra afirmação do README ficou contraditória.
- [x] 5.2 Registrar em `docs/architecture.md` o caminho do comando no service worker e a razão de usar o `windowId` do evento em vez de consultar a janela atual; verificar que o texto está coerente com `design.md`.

## 6. Validação

- [x] 6.1 Executar `npm run validate` (lint, typecheck, testes e build) e confirmar que passa integralmente.
- [x] 6.2 Carregar o pacote de `.output/chrome-mv3` no Chrome em Windows e confirmar manualmente que `Ctrl+Shift+K` abre o Quick Add com o foco no título e que `Ctrl+Shift+L` abre o Side Panel na listagem; registrar o resultado.
  - Verificado em 2026-09-18, Windows 11, Chromium 149.0.7827.55, pacote de `.output/chrome-mv3` carregado sem compactação em perfil isolado. `Ctrl+Shift+K` abre o Quick Add com o foco em "Título" e sem o campo "URL de origem"; `Ctrl+Shift+L` abre o Side Panel na listagem, sem formulário aberto. Teclas disparadas na aba em foco, que o navegador trata como acelerador de extensão.
  - **Ressalva:** em Chromium 151.0.7922.34 o navegador recusa `Ctrl+Shift+L` e `commands.getAll()` devolve combinação vazia para `open-task-manager`; `Ctrl+Shift+K` continua sendo atribuído. Em 147.0.7727.15, 148.0.7778.96 e 149.0.7827.55 a combinação é atribuída normalmente. Na mesma build 151, `Ctrl+Shift+E`, `F`, `H`, `X` e `Y` são aceitos. Decisão sobre trocar a combinação sugerida pendente de revisão humana.
- [x] 6.3 Repetir a verificação manual em macOS com `Command+Shift+K` e `Command+Shift+L`, confirmando que nenhuma das combinações é capturada pelo sistema ou pelo navegador; registrar o resultado.
  - **Não verificada.** Dispensada por decisão do responsável em 2026-09-18: não há máquina macOS disponível e a Change não ficará retida por isso. As combinações de macOS (`Command+Shift+K` e `Command+Shift+L`) permanecem declaradas no Manifest e cobertas por teste, mas nenhuma delas foi exercitada em macOS real. Vale conferir junto com a ressalva da 6.2: se `Ctrl+Shift+L` passou a ser recusado no Chromium desktop recente, `Command+Shift+L` merece a mesma checagem quando houver um macOS à mão.
- [x] 6.4 Verificar manualmente a idempotência do comando de gerenciamento: com o Side Panel aberto, com pesquisa e filtro aplicados e com um formulário de edição preenchido, acionar o atalho novamente e confirmar que nada é perdido; se o documento for remontado, registrar a evidência e levar a decisão entre ajustar a implementação ou a spec à revisão humana antes de prosseguir.
  - Verificado em 2026-09-18, Chromium 149, acionando `Ctrl+Shift+L` com o Side Panel já aberto, com pesquisa ("atalho"), filtro de status (`TODO`) e formulário de edição com texto não salvo. O documento **não** é remontado: mesmo alvo do navegador, marcador em `window` preservado, rascunho não salvo preservado, formulário ainda apresentado, pesquisa e filtro intactos. O risco levantado em `design.md` não se confirmou e a implementação atual não precisa de ajuste.
- [x] 6.5 Verificar manualmente a degradação: desatribuir o atalho de gerenciamento em `chrome://extensions/shortcuts`, confirmar que nada falha, que o bloco do Side Panel sinaliza a ausência e que o ícone e "Abrir gerenciamento" continuam funcionando; em seguida atribuir uma combinação diferente e confirmar que o bloco passa a exibi-la.
  - Verificado em 2026-09-18, Chromium 149. **Combinação ocupada:** com a sugestão apontando para uma combinação reservada pelo navegador, a extensão instala sem erro, `commands.getAll()` devolve combinação vazia para `open-task-manager`, o bloco exibe "Sem atalho atribuído" para o gerenciamento mantendo `Ctrl+Shift+K` no Quick Add, a listagem segue utilizável e "Abrir gerenciamento" continua abrindo o Side Panel. **Combinação personalizada:** com o atalho remapeado para `Ctrl+Shift+E` nas preferências do perfil (o que a tela `chrome://extensions/shortcuts` grava), o bloco passa a exibir `Ctrl+Shift+E`, e não a sugerida no pacote.
- [x] 6.6 Executar `openspec validate "adicionar-atalhos-de-teclado" --strict` e confirmar que a Change permanece válida.

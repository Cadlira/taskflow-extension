# Tasks

## 1. Domínio puro: montagem do conteúdo e interpretação da saída

- [x] 1.1 Criar em `src/domain/` as constantes nomeadas do recurso — limite de corte da descrição e teto de saída — e a função pura `buildSubtaskSuggestionContent(title, description)`, que normaliza, corta a descrição, acrescenta as instruções fixas e devolve a string final única; verificar em `tests/domain/` que a função é determinística, que a descrição é cortada no limite, que a tarefa sem descrição não transmite marcador de descrição vazia e que a saída sinaliza ao chamador que houve corte
- [x] 1.2 Garantir por teste que a string devolvida por `buildSubtaskSuggestionContent` não contém prazo, prioridade, responsável, solicitante, etiquetas, URL de origem, identificadores nem subtarefas existentes, montando a entrada a partir de uma tarefa com todos esses campos preenchidos
- [x] 1.3 Criar em `src/domain/` o parser puro da saída, que quebra por linha, descarta linhas vazias, remove marcadores de lista como hífen, asterisco e numeração e reduz repetições a uma ocorrência; verificar em `tests/domain/` cada uma dessas normalizações e o caso de nenhum item aproveitável
- [x] 1.4 Implementar o corte por vagas restantes usando `MAX_SUBTASKS` menos os itens já presentes na lista do formulário, e converter os títulos em `TaskSubtaskDraft[]` submetidos a `validateSubtaskDrafts`; verificar em `tests/domain/` que item vazio e item acima de `SUBTASK_TITLE_LIMIT` são descartados sem impedir os demais, e que a proposta nunca excede as vagas restantes

## 2. Aplicação: porta de geração e caso de uso

- [x] 2.1 Definir em `src/application/ai/` a porta de sugestão de subtarefas, recebendo o `content` já montado como valor opaco, e o tipo `AiGenerationFailure`, que reúne os motivos de `AiConnectionFailure` e acrescenta resposta vazia, resposta impossível de interpretar e resposta sem item válido; verificar com `npm run typecheck` que os tipos compilam em modo estrito
- [x] 2.2 Implementar o caso de uso que lê a configuração, confere a permissão de host da origem, recusa sem enviar quando a permissão faltar, dispara a requisição única e devolve a proposta validada ou um motivo do conjunto fechado; cobrir em `tests/application/` os desfechos de ausência de configuração, ausência de permissão, sucesso e cada motivo de falha
- [x] 2.3 Cobrir em `tests/application/` que o caso de uso repassa ao adapter exatamente a string recebida, sem reconstruir, concatenar nem reformatar, usando um adapter falso que captura o argumento
- [x] 2.4 Implementar limite de tempo no padrão já usado por `AI_CONNECTION_TIMEOUT_MS`, cancelamento por `AbortController` e bloqueio de acionamento concorrente; verificar em `tests/application/` que o tempo excedido devolve o motivo correspondente, que o cancelamento não produz proposta e que um segundo acionamento durante a requisição não gera outra chamada ao adapter
- [x] 2.5 Confirmar com `npx vitest run tests/architecture/layer-boundaries.test.ts` que nada introduzido em `src/domain` e `src/application` usa `fetch`, API Chrome, Vue, Pinia ou imports de infraestrutura

## 3. Infraestrutura: executor de geração e adapters

- [x] 3.1 Criar em `src/infrastructure/ai/` o executor de geração, separado de `runAiProbe`, que lê o corpo com limite defensivo de tamanho, extrai apenas o caminho mínimo até o texto gerado, descarta o erro capturado sem propagá-lo e mantém `redirect: 'error'`, `cache: 'no-store'`, `credentials: 'omit'` e `referrerPolicy: 'no-referrer'`; verificar em `tests/infrastructure/` cada uma dessas defesas
- [x] 3.2 Cobrir em `tests/infrastructure/` que um corpo de erro 401 contendo trecho de credencial não aparece na mensagem devolvida, no objeto de erro nem em nenhuma chamada de log, espionando o console, e que o motivo devolvido é credencial inválida
- [x] 3.3 Cobrir em `tests/infrastructure/` que um corpo de sucesso não é registrado nem persistido, e que um corpo desproporcional é interrompido pelo limite defensivo
- [x] 3.4 Implementar o adapter de geração compatível com OpenAI, usado por `OPENAI` e por `CUSTOM` com outra base, com teto de saída e sem streaming; verificar em `tests/infrastructure/ai-adapters.test.ts` a URL de destino, o cabeçalho de autorização e o corpo enviado
- [x] 3.5 Implementar o adapter de geração da Anthropic reutilizando `ANTHROPIC_VERSION` e `ANTHROPIC_DIRECT_BROWSER_HEADER` já exportados; verificar em `tests/infrastructure/ai-adapters.test.ts` que ambos os cabeçalhos são enviados, sem os quais a requisição originada de extensão é recusada
- [x] 3.6 Cobrir em `tests/infrastructure/` que uma resposta bem formada porém sem texto aproveitável devolve resposta vazia, e que uma resposta cuja estrutura não permite extrair o texto devolve resposta impossível de interpretar

## 4. Composição

- [x] 4.1 Montar os adapters de geração em `src/composition/chrome-ai-service.ts`, ao lado dos testers, mantendo `CUSTOM` no adapter compatível com OpenAI e preservando a regra de que o caminho de rede vive no Side Panel e nunca no service worker; verificar em `tests/composition/` que cada provedor recebe o adapter correto
- [x] 4.2 Confirmar por inspeção e teste que nenhum código do service worker alcança a porta de geração

## 5. Interface: ação, pré-visualização e consentimento

- [x] 5.1 Acrescentar a ação "Sugerir subtarefas" ao formulário do Side Panel, oferecida apenas com provedor configurado, e indisponível com motivo legível quando o título estiver vazio ou o limite de subtarefas já tiver sido atingido; verificar em `tests/components/tasks/` cada estado da ação
- [x] 5.2 Garantir por teste em `tests/components/` que, sem provedor configurado, nenhum elemento de assistência é apresentado no formulário e nenhuma requisição de rede é realizada
- [x] 5.3 Garantir por teste em `tests/components/quick-add/` que o popup do Quick Add não apresenta nenhuma assistência de IA mesmo com provedor configurado e permissão concedida
- [x] 5.4 Implementar a pré-visualização, exibindo o texto integral devolvido por `buildSubtaskSuggestionContent` junto da origem de destino, e a indicação de que a descrição foi cortada quando houver corte; verificar em `tests/components/` que o texto exibido é idêntico ao que o adapter recebe e que nenhuma requisição ocorre antes da confirmação
- [x] 5.5 Recompor e reapresentar a pré-visualização quando o título ou a descrição mudarem depois de exibida, exigindo nova confirmação; verificar em `tests/components/` que o conteúdo desatualizado nunca é enviado
- [x] 5.6 Implementar o consentimento de envio de conteúdo de tarefa, em memória, por origem, independente do consentimento de credencial e invalidado na troca de origem; verificar em `tests/components/` que um teste de conexão bem-sucedido anterior não dispensa o consentimento, que a recusa não envia nada e preserva o que estava digitado, e que a troca de origem exige consentir de novo
- [x] 5.7 Solicitar a permissão de host a partir do gesto do usuário quando ela faltar, sem enviar conteúdo enquanto não for concedida; verificar em `tests/components/` o caminho de permissão ausente, concedida e recusada

## 6. Interface: proposta revisável

- [x] 6.1 Apresentar a proposta como lista revisável em estado local do componente, com seleção item a item, edição do título antes de aceitar e descarte da proposta inteira; verificar em `tests/components/tasks/` a seleção parcial, a edição e o descarte
- [x] 6.2 Aceitar itens acrescentando-os a `subtaskItems` no mesmo formato `TaskSubtaskDraft` da inclusão manual, todos como não concluídos, sem remover nem reordenar as subtarefas existentes; verificar em `tests/components/tasks/` que os itens anteriores permanecem inalterados e na mesma ordem e que os aceitos continuam editáveis e removíveis
- [x] 6.3 Apresentar indicação de requisição em andamento e ação de cancelar, e informar os motivos de falha do conjunto fechado com, no máximo, origem e código de estado; verificar em `tests/components/` o progresso, o cancelamento e a apresentação de cada motivo
- [x] 6.4 Informar quando parte das sugestões foi descartada por limite de vagas e quando não houve sugestão aproveitável, sem alterar o formulário nesses casos; verificar em `tests/components/tasks/` ambos os avisos

## 7. Verificação integrada e garantias transversais

- [x] 7.1 Cobrir em `tests/integration/` o percurso completo com provedor falso: acionar, ver a pré-visualização, consentir, receber a resposta, aceitar parte dos itens e salvar, confirmando que a tarefa gravada contém as subtarefas aceitas e que a mensagem de sucesso oferece desfazer
- [x] 7.2 Cobrir em `tests/integration/` que fechar o formulário sem salvar, após aceitar itens, deixa a tarefa persistida sem esses itens, e que descartar a proposta não grava nada
- [x] 7.3 Cobrir em `tests/integration/` o caso de descrição montada a partir de seleção de página contendo texto que aparenta instruir o modelo, confirmando que o desfecho possível continua sendo apenas proposta de títulos validados, e que uma resposta que aparenta instruir mudança de endereço, modelo ou credencial não altera a configuração de provedor nem gera requisição para outro destino
- [x] 7.4 Confirmar por teste que nenhuma chave nova de armazenamento é criada ou escrita por qualquer caminho da assistência, e que o arquivo de backup exportado continua sem qualquer dado de configuração de IA ou de proposta

## 8. Permissões, gates e validação manual

- [x] 8.1 Confirmar com `npx vitest run tests/manifest` que o conjunto de permissões do Manifest permanece idêntico ao anterior a esta Change, sem nenhuma permissão nova e sem host permission fixa
- [x] 8.2 Executar `npm run lint`, `npm run typecheck` e `npx vitest run` e confirmar que todos passam
- [x] 8.3 Executar o build de produção da extensão e confirmar que conclui sem erro e sem aviso novo
- [x] 8.4 Validar manualmente no Chromium, com um provedor real ou local, o percurso completo: ausência total do recurso sem configuração, pré-visualização idêntica ao enviado, recusa de consentimento, permissão ausente, cancelamento, aceitação parcial, descarte e salvamento com desfazer; registrar as evidências
  - **Parcialmente verificada em 2026-09-20, com provedor real, porém fora do Chromium.** O percurso inteiro foi exercitado contra a DeepSeek (`https://api.deepseek.com/v1`, protocolo compatível com OpenAI, modelo `deepseek-flash`, provedor `CUSTOM`), com a composição de produção — `createChromeAiProviderService` e `createChromeAiSubtaskSuggestionService`, adapters e repository reais — e `fetch` real. Foram substituídos apenas o navegador e as APIs Chrome: DOM em `happy-dom` e APIs Chrome pelo `fake-browser` do WXT. A credencial veio de `.env`, que está no `.gitignore`, e não aparece em nenhuma evidência abaixo.
  - **Ausência total do recurso sem configuração:** com `taskflow.ai` ausente, o formulário não apresenta a ação "Sugerir subtarefas" nem nenhum painel de assistência, e o contador de requisições de rede fica em zero.
  - **Pré-visualização idêntica ao enviado:** a tarefa "Preparar a demonstração para o cliente", com descrição, produziu 458 caracteres de pré-visualização. O corpo efetivamente transmitido foi `POST https://api.deepseek.com/v1/chat/completions` com `model: "deepseek-flash"`, `max_tokens: 3000`, `stream: false` e `messages[0].content` **idêntico caractere por caractere** ao texto apresentado. Uma única requisição por acionamento.
  - **Recusa de consentimento:** com a pré-visualização apresentada e o aviso de consentimento visível, recusar não emitiu requisição alguma, e título e descrição digitados permaneceram intactos.
  - **Permissão ausente:** com `permissions.contains` devolvendo `false`, o painel apresentou o aviso de permissão e a ação "Conceder acesso a …"; com a concessão recusada, nada foi enviado e a mensagem apresentada foi "A permissão foi recusada. Nenhuma requisição foi feita e nenhuma credencial foi enviada."
  - **Cancelamento:** com uma requisição real em andamento, o cancelamento produziu "Sugestão cancelada. Nada foi alterado no formulário.", nenhuma proposta foi apresentada e a subtarefa que já existia permaneceu inalterada.
  - **Aceitação parcial e salvamento com desfazer:** sobre uma tarefa com uma subtarefa existente concluída, a resposta trouxe 18 sugestões; ao desmarcar a primeira e aceitar as demais, a tarefa gravada ficou com a subtarefa existente em primeiro lugar, ainda marcada como feita, seguida das 17 aceitas como não concluídas, e a listagem ofereceu "Desfazer".
  - **Descarte:** descartar a proposta de 18 itens e salvar gravou a tarefa sem nenhuma subtarefa, e nenhum trecho da resposta permaneceu no armazenamento.
  - **Calibração do teto de saída decidida aqui.** Com o valor inicial de 600, `deepseek-flash` — que é modelo de raciocínio — consumia todo o orçamento em `reasoning_tokens`, encerrava por `finish_reason: "length"` e devolvia `content: ""`; o TaskFlow tratava isso corretamente como resposta vazia, mas o recurso ficava inutilizável com esse modelo. Medindo 12 gerações sobre tarefas variadas, o raciocínio ficou entre 415 e 1137 tokens e a geração completa entre 597 e 1354. `SUBTASK_SUGGESTION_OUTPUT_LIMIT` passou a 3000, cerca de duas vezes o pior caso observado. O `design.md` prevê essa calibração em Open Questions, e ela não altera o contrato: o teto continua existindo e continua sendo do TaskFlow.
  - **Verificada no Chromium em 2026-09-20**, Windows 11, Chrome 153.0.8010.50, build de `.output/chrome-mv3` carregado sem empacotar (ID `ogfmokknbofabjomlfeekkceegkocacb`), contra a DeepSeek com provedor `CUSTOM`, base `https://api.deepseek.com/v1` e modelo `deepseek-flash`. A credencial foi digitada pelo próprio responsável na janela do navegador; ela não passou por nenhum script nem por nenhum log desta verificação. As leituras abaixo saíram do console da própria página do Side Panel.
  - **Sem configuração salva:** `chrome.permissions.getAll()` devolveu `{ permissions: ["activeTab","alarms","contextMenus","notifications","sidePanel","storage"], origins: [] }`, `chrome.storage.local` continha apenas `taskflow.tasks`, o Resource Timing da página não registrou nenhuma requisição fora de `chrome-extension://`, e a consulta ao DOM do formulário devolveu `temSugerir: false` com todos os pontos de ancoragem da assistência ausentes — nenhuma pré-visualização, nenhum aviso, nenhuma proposta.
  - **Verificar a conexão não transmite conteúdo:** os dois testes de conexão executados na área de provedores aparecem como `GET https://api.deepseek.com/v1/models` **sem corpo algum**.
  - **Conteúdo transmitido na geração:** uma única requisição `POST https://api.deepseek.com/v1/chat/completions`, com `model: "deepseek-flash"`, `max_tokens: 3000`, `stream: false` e `messages[0].content` contendo exatamente as instruções fixas, `Título: Preparar a demonstração para o cliente` e `Descrição: Apresentar o TaskFlow para a equipe de operações, com foco em captura rápida e lembretes.` — e nada além disso. Sem prazo, prioridade, responsável, solicitante, etiquetas, URL de origem, identificadores ou subtarefas existentes.
  - **Permissões depois da concessão:** `origins` passou a conter **exatamente** `https://api.deepseek.com/*`, nunca `https://*/*` nem `<all_urls>`, e o conjunto de `permissions` permaneceu idêntico ao anterior a esta Change.
  - **Nenhuma chave de armazenamento nova:** ao fim do percurso, `chrome.storage.local` continha apenas `taskflow.tasks` e `taskflow.ai` — a chave da configuração é da `TF-010`, e a assistência não criou nem escreveu nenhuma outra.
  - **Proposta renderizada:** a resposta produziu 17 sugestões, todas válidas pelas regras de subtarefa, apresentadas no painel "Sugestões de subtarefa" com o aviso "Revise, edite e escolha o que aceitar. Nada é gravado: a tarefa só muda quando você salvar o formulário.", caixa de seleção e título editável por item. Enquanto a proposta estava na tela, a lista de subtarefas do formulário continuava vazia: nada é aplicado sem aceitação.
  - **Confirmados por observação do responsável no navegador, sem captura instrumentada:** a pré-visualização apresentada antes de confirmar, a recusa de consentimento, o caminho de permissão ausente e sua concessão, o cancelamento durante a requisição, o descarte da proposta e o salvamento com oferta de desfazer.
- [x] 8.5 Registrar, durante a validação manual, quantas subtarefas propostas foram aceitas sem edição, editadas e descartadas em tarefas reais, como critério de decisão sobre evoluir ou abandonar o recurso, sem introduzir contador nem persistência para isso
  - **Amostra assistida de 2026-09-20, sobre tarefas realistas, não sobre as tarefas reais de um usuário.** Cinco tarefas, cinco requisições, 98 sugestões no total, todas geradas pela DeepSeek com `deepseek-flash` pelo percurso completo da interface. A classificação abaixo é um julgamento de quem conduziu a validação, não medição instrumentada: nenhum contador e nenhuma persistência foram introduzidos para isso, conforme a tarefa exige.

    | Tarefa | Sugestões | Aceitas sem edição | Editadas | Descartadas |
    | --- | ---: | ---: | ---: | ---: |
    | Publicar a nova versão da extensão na Chrome Web Store | 20 | 14 | 2 | 4 |
    | Preparar a reunião trimestral de resultados | 20 | 15 | 2 | 3 |
    | Migrar o banco de dados para a nova instância | 18 | 13 | 4 | 1 |
    | Contratar uma pessoa desenvolvedora frontend | 20 | 15 | 2 | 3 |
    | Renovar o seguro do carro | 20 | 8 | 2 | 10 |
    | **Total** | **98** | **65 (66%)** | **12 (12%)** | **21 (21%)** |

  - **Leitura.** Cerca de quatro em cada cinco sugestões foram aproveitáveis com pouco ou nenhum trabalho, o que sustenta continuar com o recurso. A qualidade, porém, não é uniforme: em tarefas procedimentais de trabalho — publicação, migração, contratação — o aproveitamento ficou entre 72% e 75% de aceitação sem edição, enquanto a tarefa pessoal curta e sem descrição ("Renovar o seguro do carro") caiu para 40%, com metade das sugestões descartadas.
  - **Principal modo de falha: decomposição excessiva.** O modelo tende a encostar no teto de 20 itens mesmo quando a tarefa comporta 6 ou 8 passos, produzindo cerimônia — "registrar aprendizados", "arquivar comprovantes", "monitorar" — e itens que se sobrepõem entre si. A confirmação item a item já é o controle previsto no desenho e absorveu isso bem: descartar é um clique. As edições, quando ocorreram, foram quase sempre para encurtar títulos longos demais, não para corrigir conteúdo errado.
  - **Nenhuma sugestão precisou ser descartada por ser incorreta ou inventada**, e nenhuma ultrapassou o limite de caracteres do título ou o limite de vagas de subtarefa — a validação compartilhada com a digitação manual não precisou descartar nada nesta amostra.
  - **Corroborado no Chromium.** A rodada no navegador real, sobre "Preparar a demonstração para o cliente", devolveu 17 sugestões, todas aproveitáveis pelas regras de subtarefa e coerentes com a tarefa — nenhuma precisou ser descartada por invalidez. O comportamento observado no navegador é o mesmo da amostra acima.
  - **Ressalva que permanece:** a classificação é julgamento de quem conduziu a validação, sobre tarefas realistas montadas para o exercício, e não sobre o acervo real de tarefas de um usuário ao longo do uso. Se a decisão de evoluir ou abandonar o recurso precisar de base mais firme, o registro deve ser repetido depois de algum tempo de uso real. Nenhum contador e nenhuma persistência foram introduzidos para isso, conforme a tarefa exige.
- [x] 8.6 Executar `openspec validate --strict` para a Change e confirmar que os artefatos estão consistentes
